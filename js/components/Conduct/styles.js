import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  conduct: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  conductTitle: {
    color: '#9963ea',
    fontSize: 16,
    marginTop: 5,
    marginBottom: 5,
    fontWeight: '500',
  },
  conductDescription: {
    fontSize: 16,
  },
});

export default styles;
